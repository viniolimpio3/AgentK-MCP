# Configuração do AgentK MCP
# Copie este arquivo para .env e configure as variáveis conforme necessário

# Chave da OpenAI para o cliente Streamlit
OPENAI_API_KEY=sua-chave-openai-aqui

# URL do servidor MCP para o cliente
MCP_SERVER_URL=http://agentk-server:3333

# Configuração Kubernetes (opcional)
# Descomente e configure conforme seu ambiente

# Para usar configuração padrão do kubectl do Windows:
# KUBECONFIG=/app/.kube/config
# E descomente no docker-compose.yml:
# - ${USERPROFILE}/.kube/config:/app/.kube/config:ro

# Para usar arquivo de configuração customizado:
# CUSTOM_KUBECONFIG_PATH=C:\caminho\para\seu\kubeconfig
# E descomente no docker-compose.yml:
# - ${CUSTOM_KUBECONFIG_PATH}:/app/.kube/config:ro

# Para Minikube no Windows, também descomente no docker-compose.yml:
# - ${USERPROFILE}/.minikube:/app/.minikube:ro